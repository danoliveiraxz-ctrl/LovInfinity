let nativePort=null;
let nativeReady=false;
const pending=new Map();
let seq=0;
function ensureNativePort(){return new Promise((resolve,reject)=>{if(nativePort&&nativeReady){resolve();return}try{nativePort=chrome.runtime.connectNative("com.lovinfinity.oauth");nativeReady=false;nativePort.onMessage.addListener(msg=>{if(msg?.id&&pending.has(msg.id)){const p=pending.get(msg.id);pending.delete(msg.id);msg.ok?p.resolve(msg):p.reject(Error(msg.error||"Erro no componente local LovInfinity."));return}if(msg?.ok)nativeReady=true});nativePort.onDisconnect.addListener(()=>{nativeReady=false;for(const p of pending.values())p.reject(Error("Componente local LovInfinity desconectado."));pending.clear();nativePort=null});nativePort.postMessage({action:"ensure_server"});const started=Date.now();const poll=()=>{if(nativeReady){resolve();return}if(Date.now()-started>5000){reject(Error("Não foi possível iniciar o componente local LovInfinity."));return}setTimeout(poll,50)};poll()}catch(e){reject(e)}})}
async function nativeRequest(action,prompt=""){await ensureNativePort();return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});nativePort.postMessage({id,action,prompt});setTimeout(()=>{if(pending.has(id)){pending.delete(id);reject(Error("Tempo limite do componente local LovInfinity."))}},10*60*1000)})}
async function setLovInfinityIcon(){try{const res=await fetch(chrome.runtime.getURL("icon.png"));const blob=await res.blob();const bitmap=await createImageBitmap(blob);const imageData={};for(const size of [16,32,48]){const canvas=new OffscreenCanvas(size,size);const ctx=canvas.getContext("2d");ctx.drawImage(bitmap,0,0,size,size);imageData[size]=ctx.getImageData(0,0,size,size)}await chrome.action.setIcon({imageData});bitmap.close()}catch(e){console.warn("LovInfinity icon error",e)}}
chrome.runtime.onInstalled.addListener(()=>{chrome.storage.local.get(["license"],d=>{if(!d.license)chrome.storage.local.set({license:null})});setLovInfinityIcon()});chrome.runtime.onStartup.addListener(setLovInfinityIcon);setLovInfinityIcon();
chrome.runtime.onMessage.addListener((msg,sender,sendResponse)=>{if(msg?.action==="ensure_lovable_host"){ensureNativePort().then(()=>sendResponse({ok:true})).catch(e=>sendResponse({ok:false,error:e.message}));return true}if(["codex_status","codex_install","codex_login","codex_mcp_login","run_codex"].includes(msg?.action)){nativeRequest(msg.action,msg.prompt||"").then(r=>sendResponse(r)).catch(e=>sendResponse({ok:false,error:e.message}));return true}});